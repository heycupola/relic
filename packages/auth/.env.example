# Convex deployment URL (defaults to localhost:3210 for development)
CONVEX_URL=http://localhost:3210

# Site URL for JWT token endpoint (defaults to localhost:3000 for development)
SITE_URL=http://localhost:3000
