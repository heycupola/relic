{
  "name": "@repo/backend",
  "module": "index.ts",
  "types": "index.d.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "bunx convex dev",
    "lint": "biome check --write .",
    "format": "biome format --write .",
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:debug": "vitest --inspect-brk",
    "test:coverage": "vitest --coverage",
    "betterAuth:generate-schema": "cd ./convex/betterAuth && better-auth generate -y --output generatedSchema.ts",
    "stripe:listen-local": "stripe listen --forward-to http://127.0.0.1:3211/webhook/stripe"
  },
  "devDependencies": {
    "@better-auth/cli": "catalog:",
    "@edge-runtime/vm": "^5.0.0",
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "convex-test": "^0.0.38",
    "vitest": "^4.0.16"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@convex-dev/better-auth": "catalog:",
    "@convex-dev/rate-limiter": "^0.2.14",
    "@convex-dev/resend": "^0.2.3",
    "@react-email/components": "^1.0.2",
    "@react-email/render": "^2.0.0",
    "@repo/crypto": "*",
    "@useautumn/convex": "^0.0.12",
    "better-auth": "catalog:",
    "convex": "catalog:",
    "convex-helpers": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:",
    "react-email": "^5.1.0",
    "resend": "^6.6.0"
  }
}
