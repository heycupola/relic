{
  "name": "@repo/cli",
  "version": "0.1.0",
  "description": "Manage project secrets the way you want with Relic – securely store and share them with your team.",
  "keywords": [
    "cli",
    "tui"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/heycupola/relic.git",
    "directory": "apps/cli"
  },
  "author": "Can Vardar",
  "license": "MIT",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build:runner": "cd ../../packages/runner && cargo build --release",
    "dev:cli": "bun run build:runner && bun run index.ts",
    "logs": "tail -f ~/.relic/logs/",
    "lint": "biome check --write .",
    "format": "biome format --write .",
    "check-types": "tsc --noEmit",
    "test": "bun test"
  },
  "files": [
    "index.ts",
    "commands/**",
    "lib/**",
    "ffi/**",
    "prebuilds/**",
    "README.md"
  ],
  "devDependencies": {
    "@repo/typescript-config": "*",
    "@types/bun": "latest",
    "bun-types": "^1.3.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "@repo/auth": "*",
    "@repo/backend": "*",
    "@repo/crypto": "*",
    "@repo/tui": "*",
    "commander": "^13.1.0",
    "convex": "catalog:",
    "ora": "^8.2.0",
    "picocolors": "^1.1.1",
    "smol-toml": "^1.6.0"
  }
}
